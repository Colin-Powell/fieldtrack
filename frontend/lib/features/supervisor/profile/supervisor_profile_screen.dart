import 'package:flutter/material.dart';

class SupervisorProfileScreen extends StatelessWidget {
  const SupervisorProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Supervisor Profile Placeholder',
        style: TextStyle(
          fontFamily: 'Poppins',
          fontSize: 24,
        ),
      ),
    );
  }
}
